unction named ‘fourbarpos’ that takes the following as inputs: 
four link lengths ( ̄l), θ1, θ2, and δ, and generates the four angles θ ̄ as output.
 Note that δ = ±1 represents either open or cross chain.